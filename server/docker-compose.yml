services:
  coupangpress:
    build: .
    ports:
      - "${PORT:-8787}:8787"
    env_file:
      - .env
    restart: unless-stopped
    shm_size: "1gb"
    environment:
      - NODE_ENV=production
      - CRAWL_MODE=server
      - HOST=0.0.0.0
